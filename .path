##### PATH #####
# Default $PATH
export PATH="$PATH:$HOME/.node/bin:/Applications/MAMP/bin/php/php5.6.2/lib/php:$HOME/.composer/vendor/bin"

# Add `~/bin` to the `$PATH`
export PATH="$HOME/bin:$PATH";

# Add laravel homestead path to the `$PATH`
export PATH="$HOME/.composer/vendor/bin:$PATH"

# Add rbenv shims to `$PATH`
export PATH="$HOME/.rbenv/bin:$PATH"

# Add sbin to $PATH
export PATH="/usr/local/sbin:$PATH"

##### $NODE_PATH #####
# Default $NODE_PATH
export NODE_PATH=$HOME/.node/lib/node_modules:/usr/local/lib/node:/usr/local/share/npm/lib/node_modules:/usr/local/lib/node_modules
